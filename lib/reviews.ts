// Shared review constraints. Reviews are stored as Markdown (plain text) in
// userRatings.review; this cap is enforced on both write routes and surfaced in
// the editor's character counter.
export const REVIEW_MAX = 4000;
